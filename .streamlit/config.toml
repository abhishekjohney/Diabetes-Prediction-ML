[theme]
# Force dark theme to match the app's custom CSS
primaryColor = "#667eea"
backgroundColor = "#0a0e27"
secondaryBackgroundColor = "#1a1f3a"
textColor = "#ffffff"
font = "sans serif"

[browser]
gatherUsageStats = false

[server]
# Automatically reload when files change
fileWatcherType = "auto"
